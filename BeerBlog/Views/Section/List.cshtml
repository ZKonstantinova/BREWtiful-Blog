@model List<BeerBlog.Models.Section>

@{
    ViewBag.Title = "List";
}

<div class="container">
    <div class="well">
        <h2>All Sections - @Html.ActionLink("Create", "Create", "Section", null, new { @class = "btn btn-warning" })</h2>
        <div class="row">
            <table class="table table-striped table-hover col-sm-12">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var ssection in Model)
                    {
                        <tr>
                            <td>@ssection.Id</td>
                            <td>@ssection.Name</td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", "Section", new { @id = ssection.Id }, null)
                                @Html.ActionLink("Delete", "Delete", "Section", new { @id = ssection.Id }, null)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
